# Bitbucket MCP Configuration Example
# Copy this file to .env and fill in your values

# Bitbucket API URL
# For Bitbucket Cloud: https://api.bitbucket.org/2.0
# For Bitbucket Server: https://your-bitbucket-server.com/rest/api/1.0
BITBUCKET_URL=https://api.bitbucket.org/2.0

# Authentication Option 1: App Password (Recommended for Bitbucket Cloud)
BITBUCKET_USERNAME=your-username
BITBUCKET_PASSWORD=your-app-password

# Authentication Option 2: Personal Access Token (Alternative)
# BITBUCKET_TOKEN=your-access-token

# Default workspace (required)
BITBUCKET_WORKSPACE=your-workspace-name

# Example configuration (replace with your actual values):
# BITBUCKET_USERNAME=your-username-here
# BITBUCKET_PASSWORD=your-app-password-here
# BITBUCKET_WORKSPACE=your-workspace-here

# Security Notes:
# 1. For Bitbucket Cloud, create an App Password at:
#    https://bitbucket.org/account/settings/app-passwords/
#    Required permissions: Repositories (Read), Pull requests (Read, Write)
#
# 2. For Bitbucket Server, you may need to use a Personal Access Token
#    Contact your administrator for the correct API URL and authentication method
#
# 3. NEVER commit the .env file with real credentials to version control
# 4. Add .env to your .gitignore file
# 5. Use environment variables in production instead of .env files
# 6. Regularly rotate your credentials
